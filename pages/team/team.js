import React from 'react';
import styles from './team.module.css';

const Team = () => {
    return(
        <div className={`${styles.container} font-mulish`}>
            <p>Inside of Teams</p>
        </div>
    );
}

export default Team;